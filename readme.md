## Tải các package và chạy server

```
    npm install
    npm run server
```

## Sử dụng file main đúng cách

1.  Nếu server cần realtime hãy dùng file `server.js`
2.  Nếu server không cần realtime hãy dùng file `index.js`
